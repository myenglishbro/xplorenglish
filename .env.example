# Cliente Supabase (browser + server). Seguros de exponer al navegador.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo para el procedimiento de bootstrap del primer admin (scripts/bootstrap-admin.md).
# NUNCA se usa en runtime de la app ni se expone al cliente.
# SUPABASE_SERVICE_ROLE_KEY=
