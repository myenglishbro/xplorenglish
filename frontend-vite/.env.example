# Cliente Supabase (browser). Seguros de exponer al navegador (anon key + RLS).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Base URL del Next.js viejo, usado solo para el Route Handler seguro de mutaciones
# (create/update/delete availability) mientras esa lógica no viva en una RPC.
VITE_API_BASE_URL=http://localhost:3000
